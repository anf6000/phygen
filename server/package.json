{
  "name": "phygen-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Controller, storage, capture, and API for the phygen evolution system",
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/index.mjs",
    "test": "node --disable-warning=ExperimentalWarning --test \"test/**/*.test.mjs\"",
    "capture-check": "node --disable-warning=ExperimentalWarning tools/capture-check.mjs"
  },
  "dependencies": {
    "@fastify/static": "8.2.0",
    "fastify": "5.6.1",
    "playwright-core": "1.56.0"
  }
}
