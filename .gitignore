node_modules/
data/
# A fresh artwork tree gets its own records directory: PHYGEN_DATA=data-simple,
# PHYGEN_DATA=data-pop1, and so on. Those are runtime state, never source.
data-*/
snapshots/
web/dist/
*.log
*.png
*.jpg
*.jpeg
*.jfif
.DS_Store
Thumbs.db
docs/recordings/
docs/videos/
*.mp4
# A consistent copy of the record and its artifacts, made before a migration.
backups/
# Local verification helpers and their screenshots. Not part of the product.
.verify/
